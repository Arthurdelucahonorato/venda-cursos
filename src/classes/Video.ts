export class Video {
    constructor(
        public nome: string,
        public titulo: string,
        public arquivo: string
    ) {}

    method(type: string): any {
        // Implementação do método genérico
    }
}
